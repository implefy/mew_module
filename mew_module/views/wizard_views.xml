<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Register Payment Wizard Form View -->
    <record id="view_sale_order_register_payment_wizard_form" model="ir.ui.view">
        <field name="name">sale.order.register.payment.wizard.form</field>
        <field name="model">sale.order.register.payment.wizard</field>
        <field name="arch" type="xml">
            <form string="Register Payment">
                <group>
                    <group>
                        <field name="order_id" readonly="1"/>
                        <field name="currency_id" invisible="1"/>
                        <field name="amount"/>
                        <field name="payment_date"/>
                    </group>
                    <group>
                        <field name="payment_reference" placeholder="e.g., Bank transfer reference"/>
                        <field name="note" placeholder="Additional notes..."/>
                    </group>
                </group>
                <footer>
                    <button name="action_register_payment"
                            string="Register Payment"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>
